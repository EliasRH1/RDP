name: Linux-XRDP-LXDE

on:
  workflow_dispatch:

jobs:
  linux-rdp:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Update system
        run: |
          sudo apt update

      - name: Install LXDE desktop
        run: |
          sudo apt install -y lxde-core lxterminal dbus-x11

      - name: Install XRDP
        run: |
          sudo apt install -y xrdp
          sudo systemctl enable xrdp

      - name: Configure XRDP for LXDE
        run: |
          # Force LXDE session
          echo "startlxde" | sudo tee /etc/skel/.xsession
          
          # Clean broken env vars and start LXDE
          sudo sed -i '1i unset DBUS_SESSION_BUS_ADDRESS\nunset XDG_RUNTIME_DIR' /etc/xrdp/startwm.sh
          sudo sed -i 's/^exec.*/exec startlxde \&/' /etc/xrdp/startwm.sh
          
          sudo systemctl restart xrdp

      - name: Create RDP user
        run: |
          sudo useradd -m rdp
          echo "rdp:rdp12345" | sudo chpasswd
          sudo usermod -aG sudo rdp
          
          sudo cp /etc/skel/.xsession /home/rdp/.xsession
          sudo chown rdp:rdp /home/rdp/.xsession

      - name: Install Tailscale
        run: |
          curl -fsSL https://tailscale.com/install.sh | sh

      - name: Connect to Tailscale
        run: |
          sudo tailscale up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-linux-lxde

      - name: Show RDP access info
        run: |
          IP=$(tailscale ip -4)
          echo ""
          echo "==============================="
          echo " XRDP + LXDE READY"
          echo " IP: $IP"
          echo " PORT: 3389"
          echo " USER: rdp"
          echo " PASS: rdp12345"
          echo " SESSION: Xorg"
          echo "==============================="
          echo ""
          sleep 999999
